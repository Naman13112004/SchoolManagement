@model SchoolManagement.Models.Subject

@{
    ViewData["Title"] = "Delete Subject";
}

<div class="row justify-content-center">
    <div class="col-lg-6 col-md-8">
        <div class="card shadow-lg border-danger rounded-lg mt-5">
            <div class="card-header bg-danger text-white">
                <h3 class="text-center my-4">Delete Confirmation 🛑</h3>
            </div>
            <div class="card-body p-4">
                <h3 class="text-danger mb-4">Are you sure you want to delete this subject?</h3>
                <p class="lead">Deleting this subject will affect all associated class schedules.</p>
                <hr />

                <dl class="row border p-3 bg-light rounded">
                    <dt class="col-sm-4 text-muted">
                        @Html.DisplayNameFor(model => model.Name)
                    </dt>
                    <dd class="col-sm-8 fw-bold text-danger">
                        @Html.DisplayFor(model => model.Name)
                    </dd>

                    <dt class="col-sm-4 text-muted">
                        Assigned Teacher
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.Teacher.FullName)
                    </dd>
                </dl>

                <form asp-action="Delete" class="mt-4">
                    <input type="hidden" asp-for="Id" />
                    <div class="d-flex justify-content-start gap-2">
                        <button type="submit" class="btn btn-danger btn-lg">
                            <i class="fas fa-trash-alt me-2"></i> Confirm Delete
                        </button>
                        <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
                            <i class="fas fa-times me-2"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>